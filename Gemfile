source "https://rubygems.org"

gem "jekyll", "~> 4.3" # This specifies a Jekyll version around 4.3
gem "minima", "~> 2.5" # This is the theme you specified in _config.yml
# Add any other gems your theme might require, but these two are typically enough for minima